# Leave empty when UI and API share the same origin (production / FastAPI serves dist/).
# For local Vite dev server, leave empty and use vite.config.js proxy to :8000.
VITE_API_URL=

# Public signup / marketing API key (optional for local dev)
VITE_PUBLIC_API_KEY=
